# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/

# Tool caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Node (demolab-web/.gitignore also covers its own build output)
node_modules/

# macOS
.DS_Store

# Short-lived run scratch (regenerated by every tool run; artifacts/ is the kept record)
temp/

# Secrets — never commit
.env
.env.local
