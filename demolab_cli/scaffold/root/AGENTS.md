> **demolab lab** — before working here, run `uv run demolab docs` and follow what it
> prints (the agent manual + runbook menu). If `demolab` is already active in the shell,
> `demolab docs` is equivalent. Use `uvx demolab-cli docs` only when bootstrapping a new
> lab with no project environment. A user message that is just a NAME in CAPS (`HELP`,
> `LINT`, `DOCTOR`, …) is a command — the manual explains.
