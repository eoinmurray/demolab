> Before editing this presentation, run `uv run demolab docs` and follow the authoring guides it
> lists. The project environment is the source of truth; do not substitute `uvx` inside an existing
> presentation.
